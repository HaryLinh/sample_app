<li>
  <%= gravatar_for user, size: Settings.size_avatar %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to t("user.delete"), user, method: :delete, data: {confirm: t("user.delete") } %>
  <% end %>
</li>
