<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items, theme: Settings.paginate_theme, params: { controller: :static_pages, action: :home } %>
<% end %>
